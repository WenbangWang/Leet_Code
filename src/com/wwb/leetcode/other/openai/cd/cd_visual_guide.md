# CD Command - Visual Guide & Flowcharts

## 🎯 The Big Picture

```
┌─────────────────────────────────────────────────────────────────┐
│                    CD COMMAND INTERVIEW                          │
│                      (45 minutes)                                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────┐
              │    Phase 1 (15 min)       │
              │  Basic Path Navigation    │
              │                           │
              │  ✓ Absolute/Relative      │
              │  ✓ . and ..               │
              │  ✓ Stack approach         │
              │  ✓ O(n) complexity        │
              └───────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────┐
              │    Phase 2 (10 min)       │
              │   Tilde Expansion         │
              │                           │
              │  ✓ ~ → /home/user         │
              │  ✓ Only at start          │
              │  ✓ String replacement     │
              └───────────────────────────┘
                              │
                              ▼
              ┌───────────────────────────┐
              │    Phase 3 (20 min)       │
              │  Symlink Resolution       │
              │                           │
              │  ✓ Longest prefix match   │
              │  ✓ Cycle detection        │
              │  ✓ Iterative resolution   │
              │  ✓ HashMap or Trie        │
              └───────────────────────────┘
```

---

## 📊 Phase 1: Stack-Based Path Navigation

### Visual Example: `cd("/home/user", "../docs/./photos")`

```
Step 1: Combine paths
┌──────────────────────────────────────┐
│ currentDir: /home/user               │
│ targetDir:  ../docs/./photos         │
│                                      │
│ Combined: /home/user/../docs/./photos│
└──────────────────────────────────────┘

Step 2: Split by "/"
┌────────────────────────────────────────────────┐
│ ["", "home", "user", "..", "docs", ".", "photos"] │
└────────────────────────────────────────────────┘

Step 3: Process with Stack
┌──────────┬──────────┬────────────────┬──────────────┐
│ Token    │ Action   │ Stack          │ Explanation  │
├──────────┼──────────┼────────────────┼──────────────┤
│ ""       │ Skip     │ []             │ Empty        │
│ "home"   │ Push     │ [home]         │ Regular dir  │
│ "user"   │ Push     │ [home, user]   │ Regular dir  │
│ ".."     │ Pop      │ [home]         │ Go up        │
│ "docs"   │ Push     │ [home, docs]   │ Regular dir  │
│ "."      │ Skip     │ [home, docs]   │ Current dir  │
│ "photos" │ Push     │ [home, docs,   │ Regular dir  │
│          │          │  photos]       │              │
└──────────┴──────────┴────────────────┴──────────────┘

Step 4: Build result
┌──────────────────────────┐
│ Stack: [home, docs, photos] │
│ Result: /home/docs/photos   │
└──────────────────────────┘
```

### Edge Case: Going Above Root

```
cd("/home", "../../..")

Stack Processing:
┌────────┬────────┬───────────┬──────────────────┐
│ Token  │ Action │ Stack     │ Status           │
├────────┼────────┼───────────┼──────────────────┤
│ "home" │ Push   │ [home]    │ OK               │
│ ".."   │ Pop    │ []        │ OK               │
│ ".."   │ Pop?   │ []        │ Stack empty!     │
│        │        │           │ Return null      │
└────────┴────────┴───────────┴──────────────────┘

Result: null (cannot go above root)
```

---

## 🏠 Phase 2: Tilde Expansion

### Decision Tree

```
                    targetDir
                        │
                        ▼
              ┌─────────────────────┐
              │ Starts with "~"?    │
              └─────────────────────┘
                  │             │
                  │ Yes         │ No
                  ▼             ▼
         ┌──────────────┐   Use as-is
         │ Exactly "~"? │
         └──────────────┘
            │         │
            │ Yes     │ No
            ▼         ▼
    targetDir =   ┌──────────────────┐
     homeDir      │ Starts with "~/"? │
                  └──────────────────┘
                      │           │
                      │ Yes       │ No
                      ▼           ▼
              targetDir =    Treat as
              homeDir +      literal
              targetDir.     directory
              substring(1)   named "~something"
```

### Examples

```
┌────────────────┬──────────────┬───────────────────┬─────────────────┐
│ currentDir     │ targetDir    │ homeDir           │ Result          │
├────────────────┼──────────────┼───────────────────┼─────────────────┤
│ /etc           │ ~            │ /home/user        │ /home/user      │
│ /etc           │ ~/docs       │ /home/user        │ /home/user/docs │
│ /etc           │ ~alice       │ /home/user        │ /etc/~alice     │
│ /home          │ test/~/file  │ /home/user        │ /home/test/~/file│
└────────────────┴──────────────┴───────────────────┴─────────────────┘
                                                     │
                                              (~ not at start,
                                               treated as literal)
```

---

## 🔗 Phase 3: Symlink Resolution

### Algorithm Flow

```
┌─────────────────────────┐
│ Start: Normalized Path  │
│ /home/link/docs/file    │
└─────────────────────────┘
           │
           ▼
┌─────────────────────────────────┐
│ visited = {}                    │
│ iterations = 0                  │
│ maxIterations = symlinks.size+1 │
└─────────────────────────────────┘
           │
           ▼
      ┌────────────────────┐
      │ iterations <       │◀─────────┐
      │ maxIterations?     │          │
      └────────────────────┘          │
           │        │                 │
           │ Yes    │ No              │
           ▼        ▼                 │
    ┌──────────┐  DONE               │
    │ Add to   │                     │
    │ visited  │                     │
    └──────────┘                     │
           │                         │
           ▼                         │
    ┌──────────────┐                │
    │ Already in   │                │
    │ visited?     │                │
    └──────────────┘                │
           │        │                │
           │ Yes    │ No             │
           ▼        │                │
       CYCLE!       │                │
       (throw)      │                │
                    ▼                │
         ┌─────────────────────┐    │
         │ Find longest        │    │
         │ matching symlink    │    │
         └─────────────────────┘    │
                    │                │
           ┌────────┴────────┐      │
           │ No    │ Found   │      │
           ▼       ▼         │      │
         DONE   Replace      │      │
                prefix with  │      │
                target       │      │
                    │        │      │
                    ▼        │      │
              Normalize      │      │
              new path       │      │
                    │                │
                    └────────────────┘
```

### Example: Resolving Chained Symlinks

```
Symlinks:
  /home/link1 → /etc
  /etc/conf   → /var/config

Path: /home/link1/conf/app.yml

┌─────────────────────────────────────────────────────────────┐
│ Iteration 1: /home/link1/conf/app.yml                       │
├─────────────────────────────────────────────────────────────┤
│ Check: /home              → No symlink                       │
│ Check: /home/link1        → MATCH! Target: /etc            │
│ Check: /home/link1/conf   → No (stop at first match)       │
│                                                             │
│ Replace: /etc + /conf/app.yml = /etc/conf/app.yml          │
│ visited: {"/home/link1/conf/app.yml"}                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Iteration 2: /etc/conf/app.yml                              │
├─────────────────────────────────────────────────────────────┤
│ Check: /etc              → No symlink                        │
│ Check: /etc/conf         → MATCH! Target: /var/config      │
│                                                             │
│ Replace: /var/config + /app.yml = /var/config/app.yml      │
│ visited: {"/home/link1/conf/app.yml", "/etc/conf/app.yml"} │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ Iteration 3: /var/config/app.yml                            │
├─────────────────────────────────────────────────────────────┤
│ Check: /var              → No symlink                        │
│ Check: /var/config       → No symlink                        │
│                                                             │
│ No symlink found → DONE                                     │
│ Result: /var/config/app.yml                                 │
└─────────────────────────────────────────────────────────────┘
```

### Cycle Detection

```
Symlinks:
  /a → /b
  /b → /c
  /c → /a

Path: /a/file.txt

┌────────────────────────────────────────────────┐
│ Iteration 1: /a/file.txt                       │
│ Replace: /b/file.txt                           │
│ visited: {"/a/file.txt"}                      │
└────────────────────────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────────┐
│ Iteration 2: /b/file.txt                       │
│ Replace: /c/file.txt                           │
│ visited: {"/a/file.txt", "/b/file.txt"}       │
└────────────────────────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────────┐
│ Iteration 3: /c/file.txt                       │
│ Replace: /a/file.txt                           │
│ visited: {"/a/file.txt", "/b/file.txt",       │
│           "/c/file.txt"}                       │
└────────────────────────────────────────────────┘
                    │
                    ▼
┌────────────────────────────────────────────────┐
│ Iteration 4: /a/file.txt                       │
│                                                │
│ Check visited: "/a/file.txt" already in set!  │
│ → CYCLE DETECTED                               │
│ → throw RuntimeException                       │
└────────────────────────────────────────────────┘
```

---

## 🔀 HashMap vs Trie Comparison

### HashMap Approach: Longest Prefix Matching

```
Path: /home/user/docs/file.txt
Symlinks: {"/home/link" → "/etc", "/home/user/docs" → "/shared"}

┌─────────────────────────────────────────────────────────────┐
│ Build prefixes and check each:                              │
├─────────────────────────────────────────────────────────────┤
│ Prefix: /home           Check symlinks: No match           │
│ Prefix: /home/user      Check symlinks: No match           │
│ Prefix: /home/user/docs Check symlinks: MATCH! (/shared)   │  ⭐
│ Prefix: /home/user/docs/file.txt  (continue to find longest) │
│                                                             │
│ Longest match: /home/user/docs → /shared                   │
│ Result: /shared/file.txt                                    │
└─────────────────────────────────────────────────────────────┘

Complexity: O(path_segments * num_symlinks)
            = O(n * m)
```

### Trie Approach: Single Traversal

```
Symlinks Trie:
        root
         │
      ┌──┴──┐
    home   etc
      │
    user
      │
    docs ← pathTokens = ["shared"]
    
Path: /home/user/docs/file.txt
Tokens: [home, user, docs, file.txt]

┌─────────────────────────────────────────────────────────────┐
│ Traverse Trie once:                                          │
├─────────────────────────────────────────────────────────────┤
│ Token: home      → Trie: root→home        (no symlink)     │
│ Token: user      → Trie: home→user        (no symlink)     │
│ Token: docs      → Trie: user→docs        (SYMLINK!)  ⭐   │
│                     pathTokens = ["shared"]                 │
│ Token: file.txt  → (no child in trie, stop traversal)      │
│                                                             │
│ Longest match: docs at index 2 → ["shared"]                │
│ Remaining: [file.txt]                                       │
│ Result: ["shared", "file.txt"] = /shared/file.txt          │
└─────────────────────────────────────────────────────────────┘

Complexity: O(path_segments) = O(n)
```

### Side-by-Side Performance

```
Scenario: 1000 symlinks, path with 10 segments

HashMap Approach:
┌──────────────────────────────────┐
│ Segment 1: Check 1000 symlinks  │  = 1000 checks
│ Segment 2: Check 1000 symlinks  │  = 1000 checks
│ Segment 3: Check 1000 symlinks  │  = 1000 checks
│ ...                              │
│ Segment 10: Check 1000 symlinks │  = 1000 checks
└──────────────────────────────────┘
Total: 10,000 checks

Trie Approach:
┌──────────────────────────────────┐
│ Segment 1: Traverse trie         │  = 1 step
│ Segment 2: Traverse trie         │  = 1 step
│ Segment 3: Traverse trie         │  = 1 step
│ ...                              │
│ Segment 10: Traverse trie        │  = 1 step
└──────────────────────────────────┘
Total: 10 steps

Speedup: 1000×
```

---

## ⏱️ Time Management Visualization

### Ideal Interview Timeline (45 minutes)

```
0min                                                        45min
├──────────┬─────────┬─────────────────────┬────────┬──────┤
│ Clarify  │ Phase 1 │     Phase 2         │ Phase 3│Buffer│
│ 5min     │ 15min   │     10min           │ 20min  │ 5min │
└──────────┴─────────┴─────────────────────┴────────┴──────┘

Phase 1 Breakdown (15 min):
0     3     6     9    12   15
├─────┬─────┬─────┬─────┬───┤
│Code │Test │Debug│Edge │Buf│
      │cases│     │cases│fer│

Phase 2 Breakdown (10 min):
0    3    6    8   10
├────┬────┬────┬───┤
│Code│Test│Edge│Buf│

Phase 3 Breakdown (20 min):
0    5    10   15   18  20
├────┬─────┬────┬────┬──┤
│Plan│Code │Test│Edge│Buf│
│Out │     │    │cases│  │
```

### Red Flags: Time Traps to Avoid

```
❌ DANGER ZONE

┌────────────────────────────────────────┐
│ Phase 1 taking > 20 minutes            │
│ ──────────────────────────────────────►│
│ You're: - Over-engineering             │
│         - Missing key insight (stack)  │
│         - Debugging too long           │
│                                        │
│ ACTION: Ask for hint, move forward    │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Implementing Trie in Phase 3           │
│ ──────────────────────────────────────►│
│ Risk: Won't finish in 20 minutes      │
│                                        │
│ ACTION: Use HashMap, discuss Trie     │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ Silent coding for > 5 minutes          │
│ ──────────────────────────────────────►│
│ Interviewer can't assess thinking     │
│                                        │
│ ACTION: Explain as you code           │
└────────────────────────────────────────┘
```

---

## 🎓 Complexity Cheat Sheet

### Quick Reference Table

```
┌─────────┬──────────────┬─────────────┬────────────────┐
│ Phase   │ Time         │ Space       │ Key Operation  │
├─────────┼──────────────┼─────────────┼────────────────┤
│ 1       │ O(n)         │ O(n)        │ Stack push/pop │
│ 2       │ O(n)         │ O(n)        │ String replace │
│ 3 (Hash)│ O(k*m*n)     │ O(n)        │ HashMap lookup │
│ 3 (Trie)│ O(k*n)       │ O(total     │ Trie traversal │
│         │              │  chars in   │                │
│         │              │  symlinks)  │                │
└─────────┴──────────────┴─────────────┴────────────────┘

Where:
  n = total path length
  m = number of symlinks
  k = symlink resolution iterations (bounded by m+1)
  
For typical inputs:
  n ≈ 50-100 characters
  m ≈ 10-50 symlinks
  k ≈ 2-3 iterations
```

### When to Mention What

```
┌───────────────────────────────────────────────────────────┐
│ After Phase 1:                                            │
│ "The time complexity is O(n) where n is the total path   │
│  length, and space is O(n) for the stack."               │
└───────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────┐
│ After Phase 3 (HashMap):                                  │
│ "For symlink resolution, this is O(k*m*n) where k is     │
│  the number of resolution iterations (bounded), m is the │
│  number of symlinks, and n is the path length. For many  │
│  symlinks, we could optimize to O(k*n) using a Trie."    │
└───────────────────────────────────────────────────────────┘
```

---

## 🎯 Success Signals Checklist

### Visual Progress Tracker

```
Phase 1: Basic Navigation
└─ [ ] Implemented in < 20 minutes
   [ ] All basic tests passing
   [ ] Handles . and ..
   [ ] Checks root boundary
   [ ] O(n) complexity stated
   [ ] Code is clean and readable

Phase 2: Tilde Expansion  
└─ [ ] Added in < 10 minutes
   [ ] Only expands ~ at start
   [ ] Tests for ~/path working
   [ ] Edge case: ~ in middle handled

Phase 3: Symlinks
└─ [ ] Correct approach chosen (HashMap)
   [ ] Longest prefix matching working
   [ ] Cycle detection implemented
   [ ] Bounded iterations (safety)
   [ ] At least partial implementation

Communication
└─ [ ] Explained approach before coding
   [ ] Tested after each phase
   [ ] Asked clarifying questions
   [ ] Discussed trade-offs
   [ ] Handled edge cases proactively
```

### Strong Hire Signal

```
✅ ✅ ✅ ✅ ✅ ✅   Phase 1 perfect
✅ ✅ ✅ ✅        Phase 2 perfect
✅ ✅ ✅ ✅ ✅     Phase 3 working
✅ ✅ ✅          Good communication
✅ ✅             Trade-off discussion

OUTCOME: Strong Hire
```

### Hire Signal

```
✅ ✅ ✅ ✅ ✅     Phase 1 perfect
✅ ✅ ✅ ✅        Phase 2 perfect
✅ ✅ ⬜ ⬜ ⬜     Phase 3 partial
✅ ✅ ✅          Good communication
✅                Some discussion

OUTCOME: Hire (with some reservations)
```

---

## 🚀 Visual Study Plan

```
Week 1: Foundation
┌─────┬─────┬─────┬─────┬─────┬─────┬─────┐
│ Mon │ Tue │ Wed │ Thu │ Fri │ Sat │ Sun │
├─────┼─────┼─────┼─────┼─────┼─────┼─────┤
│Read │Code │Code │Rest │Read │Code │Code │
│All  │P1+2 │P1-3 │     │Adv  │Full │Full │
│Docs │     │     │     │Docs │Impl │Impl │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┘

Day Before Interview:
┌──────────┬──────────┬──────────┬──────────┐
│ Morning  │   Noon   │ Evening  │  Night   │
├──────────┼──────────┼──────────┼──────────┤
│ Practice │   Rest   │  Review  │  Light   │
│ Full     │          │  Cheat   │  Review  │
│ Impl     │          │  Sheet   │          │
└──────────┴──────────┴──────────┴──────────┘

Interview Day:
┌──────────┬──────────┬──────────┬──────────┐
│ Morning  │  1h Bef  │ 10m Bef  │Interview │
├──────────┼──────────┼──────────┼──────────┤
│  Rest    │  Quick   │  Cheat   │   GO!    │
│          │  Review  │  Sheet   │          │
└──────────┴──────────┴──────────┴──────────┘
```

---

## 💡 Mental Models

### The Stack Model (Phase 1)

```
Think of directories as a stack of folders:

Physical Stack:          Code Stack:
┌─────────┐              
│ photos  │  ← top       stack.push("photos")
├─────────┤              
│  docs   │              stack.push("docs")
├─────────┤              
│  home   │  ← bottom    stack.push("home")
└─────────┘              

Going up (..)            stack.pop()
means removing top:      
┌─────────┐              
│  docs   │  ← new top   
├─────────┤              
│  home   │              
└─────────┘              
```

### The Graph Model (Phase 3)

```
Think of symlinks as directed graph edges:

/a → /b → /c

┌───┐      ┌───┐      ┌───┐
│ /a│─────▶│ /b│─────▶│ /c│
└───┘      └───┘      └───┘

Cycle:
┌───┐      ┌───┐
│ /a│─────▶│ /b│
└───┘      └───┘
  ▲          │
  │          │
  └──────────┘

Use visited set to detect cycles!
```

---

This visual guide complements the detailed documentation. Use it for:
- Quick understanding of algorithms
- Time management during interview
- Visual learningof complex concepts
- Last-minute review

Good luck! 🎉

